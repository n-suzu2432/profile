# GitHubにプッシュできなかった原因と解決方法

---

## おきたこと（全部で3つのトラブル）

---

## トラブル① スペースが抜けてた

### なにをしたか
```
git add.
```

### なぜダメだったか
`add` と `.` の間にスペースがない。

Gitへの命令は「単語と単語の間にスペース」が必要。  
`add.` という命令は存在しないので、Gitが「そんな命令知らない」とエラーを出した。

### 解決方法
```
git add .
```
スペースを1つ入れるだけでOK！

---

## トラブル② `main` じゃなくて `master` だった

### なにをしたか
```
git push origin main
```

### なぜダメだったか
Gitでは「ブランチ」という**作業場所に名前をつける**しくみがある。  
昔は **`master`**、最近は **`main`** という名前が多い。

`portfolio` フォルダの作業場所は **`master`** という名前なのに、  
`main` に送ろうとしたので「そんな場所ない！」とエラーになった。

### 解決方法
```
git push origin master
```
正しい名前で送ればOK！

---

## トラブル③ 送り先（GitHub）が登録されていなかった

### なにをしたか
```
git push origin master
```

### なぜ `.git` が2つになったか
`portfolio` フォルダは `create-next-app`（Next.jsのセットアップコマンド）で作った。  
このコマンドには「プロジェクトを作ったら自動でGit管理も始める」という機能があり、  
勝手に `.git` フォルダを作ってしまう。

親フォルダ（SKILL BOOST）でもすでにGit管理していたので、`.git` が2重になった。

```
① npx create-next-app で portfolio フォルダを作成
      ↓
② create-next-app が自動で git init を実行（.git が自動生成される）
      ↓
③ 親フォルダ（SKILL BOOST）でも git init していた
      ↓
④ 結果：.git が2重に存在してしまった ← これが原因！
```

### なぜダメだったか
`portfolio` フォルダの中に、こっそり別の `.git` フォルダが入っていた。  
そのせいでGitが「`portfolio` は別のリポジトリ（別の引き出し）だ」と思い込んでしまった。

この「内側の引き出し」にはGitHubの送り先が登録されていなかったので、  
「送り先が見つからない！」とエラーになった。

ざっくりイメージ：

```
大きな引き出し（profile リポジトリ）
└── portfolio フォルダ
    └── .git ← ここに「小さな引き出し」が入っていた！（これが原因）
```

### なぜ2つあるとダメなのか
大きな引き出しの中に、小さな引き出しが入っているイメージ。

```
大きな引き出し（SKILL BOOST）
└── portfolio フォルダ
    └── 小さな引き出し（portfolio の .git）
```

Gitは引き出しの中身を全部まとめてGitHubに送る。  
でも小さな引き出しを見つけると、

「これは別の人の引き出しだ。勝手に開けちゃダメ！」

と判断して、**中身を無視**してしまう。  
だから `git add .` しても `portfolio` の中のファイルが追加されず、GitHubにも送れなかった。

### 解決方法
**3ステップで直した**

**ステップ1：** `portfolio` の中の `.git`（小さな引き出し）を削除
```
Remove-Item -Recurse -Force .git
```

**ステップ2：** 大きな引き出しから「サブモジュール登録」を解除して、普通のフォルダとして登録し直す
```
git rm --cached portfolio
git add portfolio/
```

**ステップ3：** コミットしてGitHubに送る
```
git commit -m "add portfolio files"
git push origin main
```

---

## まとめ

| トラブル | 原因 | 解決 |
|---------|------|------|
| `git add.` | スペースが抜けてた | `git add .` にする |
| `main` でエラー | ブランチ名が `master` だった | `git push origin master` にする |
| 送り先がない | フォルダの中に別の `.git` が入っていた | `.git` を削除して登録し直す |

---

## 今後のために覚えておくこと

- `git add .` のスペースを忘れない
- ブランチ名は `git branch` で確認できる
- フォルダの中に別の `.git` を作らない（1つのプロジェクト = 1つの `.git`）
- `create-next-app` を使うときは `--no-git` をつけると `.git` が自動生成されない
  ```
  npx create-next-app --no-git
  ```
